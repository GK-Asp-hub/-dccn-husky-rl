# Минимальные зависимости для запуска экспериментов
# Воспроизведённое окружение — см. requirements-lock.txt (точные версии, на которых получены результаты статьи)
# Python 3.11+

numpy>=2.0,<3.0
scipy>=1.15
matplotlib>=3.10
gymnasium>=1.0,<2.0
pybullet>=3.2.6
stable-baselines3>=2.7,<3.0
# torch ставится автоматически как зависимость stable-baselines3 (CPU-версии достаточно)

# Для запуска тестов
pytest>=8.0
